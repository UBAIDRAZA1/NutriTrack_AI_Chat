# React + Vite Project

This is a simple React project built using Vite.

## 🚀 How to Run

Follow the steps below to run this project locally:

### 1. Clone the Repository

```bash
git clone <https://github.com/UBAIDRAZA1/Food_website.git>


2. Navigate to the Project Directory
cd <project-folder>

3. Install Dependencies
npm install

4. Start the Development Server
for frontend run
npm run dev

for backend run 
node index.js

5. Open in Browser
http://localhost:5173



6. Install Dependencies:(Frontend)

npm install react-router-dom lucide-react
npm install react-router-dom
npm install framer-motion
npm install recharts lucide-react @google/generative-ai
npm install recharts lucide-react @google/generative-ai quagga
npm install recharts lucide-react @google/generative-ai
npm install react-router-dom recharts lucide-react framer-motion @google/generative-ai react-helmet-async jspdf
npm install react recharts lucide-react @google/generative-ai quagga axios
npm install axios
npm list axios react-router-dom
npm install react-icons
npm install styled-jsx
npm install cors express





### For Backend 
npm list express mongoose cors bcryptjs jsonwebtoken dotenv
npm install express mongoose bcryptjs jsonwebtoken cors dotenv axios

### stop backend process
Stop-Process -Name "node" -Force
"# NutriTrack_AI_Chat" 
